|=================|
schema Person: {
  name: string,
  active: boolean
}
Person person = {
  name: "Ada",
  active: true
};
|=================|
[output = 'data', schema = Person]
{
  name: person.name,
  active: person.active
}