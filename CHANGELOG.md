# Changelog

## [Unreleased]

- Add Tree-sitter-parity TextMate highlighting, snippets, indentation, and language configuration.
- Start the Mace language server for `.mace` files.
- Download and cache the pinned Mace release for supported platforms.
- Add a development mode that runs `go run ./cmd lsp` from the workspace.
- Add language-server commands, configuration, context menus, a restart keybinding, output logs, progress reporting, server selection, and status-bar state.
